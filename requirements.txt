# The prototype and the benchmark run with no dependencies at all: the heuristic
# reasoner is stdlib-only, and so are the OpenAI-compatible model tiers (groq,
# together, openrouter, local), which speak plain HTTP.
#
# Install this only for --reasoner claude, which also needs ANTHROPIC_API_KEY (or
# an `ant auth login` profile). Without credentials any model tier falls back to
# heuristics and says so rather than pretending.
anthropic>=1.0.0
